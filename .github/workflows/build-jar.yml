# CC-Tweaked 中文版 - 构建工作流
# 自动构建 NeoForge 和 Fabric 版本的 JAR 文件

name: Build JAR

on:
  push:
    branches: [ mc-1.21.x ]
  pull_request:
    branches: [ mc-1.21.x ]
  workflow_dispatch:

permissions:
  contents: read
  packages: none

jobs:
  build-neoforge:
    name: Build NeoForge JAR
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Clone repository
      uses: actions/checkout@v4

    - name: 📥 Set up Java
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: 'gradle'

    - name: 📥 Setup Gradle
      uses: gradle/actions/setup-gradle@v4

    - name: ⚒️ Build NeoForge
      run: ./gradlew :neoforge:build --no-daemon

    - name: 📦 Find NeoForge JAR
      id: find-neoforge-jar
      run: |
        JAR_PATH=$(find projects/neoforge/build/libs -type f -name "*.jar" ! -name "*-sources.jar" ! -name "*-dev.jar" | head -1)
        echo "neoforge-jar=$JAR_PATH" >> $GITHUB_OUTPUT
        echo "Found: $JAR_PATH"

    - name: 📤 Upload NeoForge JAR
      uses: actions/upload-artifact@v4
      with:
        name: CC-Tweaked-NeoForge
        path: ${{ steps.find-neoforge-jar.outputs.neoforge-jar }}
        retention-days: 7

  build-fabric:
    name: Build Fabric JAR
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Clone repository
      uses: actions/checkout@v4

    - name: 📥 Set up Java
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: 'gradle'

    - name: 📥 Setup Gradle
      uses: gradle/actions/setup-gradle@v4

    - name: ⚒️ Build Fabric
      run: ./gradlew :fabric:build --no-daemon

    - name: 📦 Find Fabric JAR
      id: find-fabric-jar
      run: |
        JAR_PATH=$(find projects/fabric/build/libs -type f -name "*.jar" ! -name "*-sources.jar" ! -name "*-dev.jar" | head -1)
        echo "fabric-jar=$JAR_PATH" >> $GITHUB_OUTPUT
        echo "Found: $JAR_PATH"

    - name: 📤 Upload Fabric JAR
      uses: actions/upload-artifact@v4
      with:
        name: CC-Tweaked-Fabric
        path: ${{ steps.find-fabric-jar.outputs.fabric-jar }}
        retention-days: 7

  build-all:
    name: Build All JARs
    runs-on: ubuntu-latest
    needs: [build-neoforge, build-fabric]
    
    steps:
    - name: 📥 Clone repository
      uses: actions/checkout@v4

    - name: 📥 Set up Java
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: 'gradle'

    - name: 📥 Setup Gradle
      uses: gradle/actions/setup-gradle@v4

    - name: ⚒️ Build All
      run: ./gradlew assemble --no-daemon

    - name: 📦 Prepare All JARs
      run: |
        mkdir -p jars
        # Copy NeoForge JARs
        find projects/neoforge/build/libs -type f -name "*.jar" ! -name "*-sources.jar" ! -name "*-dev.jar" -exec cp {} jars/ \;
        # Copy Fabric JARs
        find projects/fabric/build/libs -type f -name "*.jar" ! -name "*-sources.jar" ! -name "*-dev.jar" -exec cp {} jars/ \;
        ls -la jars/

    - name: 📤 Upload All JARs
      uses: actions/upload-artifact@v4
      with:
        name: CC-Tweaked-All-JARs
        path: ./jars
        retention-days: 30
