gps 可用于托管 GPS 服务器，或使用三边测量法确定位置。
输入 "help gpsapi" 获取在 Lua 程序中使用 GPS 函数的帮助信息。

示例：
"gps locate" 会连接附近的 GPS 服务器，并尝试确定计算机或 turtles 的位置。
"gps host" 会尝试确定位置，并在成功时托管一个 GPS 服务器。
"gps host 10 20 30" 将托管一个 GPS 服务器，使用手动输入的位置 10,20,30。

手动输入主机位置时请注意。如果多个 GPS 主机的位置不一致，
locate 调用的结果将会不正确。
